Tiny numeric badge that rides next to a mono nav link or label.

```jsx
<span className="hp-label">Projects</span> <CounterPill count={68} />
```

`tone`: `orange` (default) · `ink` · `soft` (orange wash).
