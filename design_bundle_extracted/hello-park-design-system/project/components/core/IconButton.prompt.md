Circular icon button — defaults to the brand chevron "›"; use for next/forward affordances and compact actions.

```jsx
<IconButton label="Next" />
<IconButton variant="ghost" label="Next" />
```

Variants: `primary` (orange) · `ink` · `ghost` (outline). Sizes `sm/md/lg`. Always pass `label` for a11y.
